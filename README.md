Description:
Simple server with frontend to practice AWS EC2, Load Balancing, Security Groups, Target Groups, Networking, Docker and Docker Compose.

Running the server:
```DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 docker compose up -d --build```

